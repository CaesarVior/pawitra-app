server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index views/home/index.html index.html;

    location = / {
        try_files /views/home/index.html =404;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}